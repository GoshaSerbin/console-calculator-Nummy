#include "Formatter.hpp"

namespace Nummy {

    auto Formatter::formate(const std::string& message, MessageType type) -> std::string {
        switch (type) {
            default:
                break;
        }
        return message;
    }

}  // namespace Nummy
